# accounting-app

This SwiftUI project demonstrates a small budgeting application. The
`OpenAIManager` component requires an API key for network requests. Set the
`OPENAI_API_KEY` environment variable before running the app so that the
OpenAI service can be reached securely.
